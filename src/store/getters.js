const getters = {
    currentType(state) {
        return state.currentType
    },
    warNewsList(state) {
        return state.warNewsList
    },
    isLoading(state) {
        return state.isLoading
    }
}
export default getters
